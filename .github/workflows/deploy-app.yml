name: deploy flask app

on:
    workflow_dispatch:

jobs:
    deploy:
        runs-on: self-hosted
        env:
            DOCKERHUB_USR: ${{ vars.DOCKERHUB_USR }}

        steps:
          - name: checkout code
            uses: actions/checkout@v4

          - name: deploy with docker compose
            run: docker-compose up -d --build